---
phase: 05-dark-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/index.html
autonomous: true
requirements:
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Opening the app shows a dark background (#0f1419) with light text — no white or light-grey backgrounds visible anywhere in the layout"
    - "All interactive elements (inputs, buttons, select, textarea, session list) render within the dark theme using surface/border/text CSS variables"
    - "After clip extraction completes, the filename field is empty and the description textarea is reset to the template without a page reload"
    - "If a video duration is known, the scrubber range resets to full video span (0 to duration) after extraction"
  artifacts:
    - path: "static/index.html"
      provides: "Dark theme CSS variables + resetFormAfterExtract() JS function"
      contains: "--bg: #0f1419"
    - path: "static/index.html"
      provides: "Post-extract reset wiring inside onClipSubmit"
      contains: "resetFormAfterExtract"
  key_links:
    - from: "onClipSubmit (static/index.html)"
      to: "resetFormAfterExtract()"
      via: "called after successful POST /api/clips response"
      pattern: "resetFormAfterExtract"
    - from: "CSS :root block"
      to: "all form elements"
      via: "var(--bg), var(--surface), var(--surface2), var(--border), var(--text)"
      pattern: "background: var\\(--bg\\)"
---

<objective>
Replace the light form UI with a dark minimal theme and add post-extraction form reset to static/index.html.

Purpose: Fast clip stamping requires zero friction between extractions — dark theme reduces eye fatigue during long sessions, and auto-reset removes the manual step of clearing the filename field.
Output: A single updated static/index.html that passes both UI-03 (dark theme) and UI-04 (post-extract reset).
</objective>

<execution_context>
@/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@static/index.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply dark minimal theme to index.html</name>
  <files>static/index.html</files>
  <action>
    Replace the existing CSS with a dark design system using these exact token values:

    ```css
    :root {
      --bg: #0f1419;
      --surface: #1a222d;
      --surface2: #232d3a;
      --border: #334155;
      --text: #e8eef5;
      --muted: #94a3b8;
      --accent: #38bdf8;
      --accent-dim: rgba(56, 189, 248, 0.15);
      --ok: #4ade80;
      --warn: #fbbf24;
      --radius: 12px;
      --font: "DM Sans", system-ui, sans-serif;
      --mono: "JetBrains Mono", ui-monospace, monospace;
    }
    ```

    Apply to every element:
    - body: background var(--bg), color var(--text)
    - section cards: background var(--surface), border 1px solid var(--border), border-radius var(--radius)
    - inputs, select, textarea: background var(--surface2), border 1px solid var(--border), color var(--text)
    - .btn-primary: background var(--accent), color #0f172a (dark text on light button)
    - .btn-ghost: background var(--surface2), color var(--text), border 1px solid var(--border)
    - .btn-danger: background transparent, color #f87171, border 1px solid rgba(248,113,113,0.45)
    - Status pills: pill-ok uses --ok tones, pill-warn uses --warn tones, pill-muted uses --accent tones
    - Clip list items: border-bottom var(--border), text uses --text and --muted
    - Cache panel (details.cache-panel): background var(--surface2), border var(--border)
    - Timeline track: background var(--surface2), border var(--border)
    - Timeline range: background var(--accent-dim)
    - Timeline handles: background var(--accent), border #0ea5e9

    Add Google Fonts preconnect + import for DM Sans and JetBrains Mono in the <head>.

    Verify no hardcoded white (#fff, #ffffff, white) or light-grey backgrounds appear anywhere in the stylesheet.
  </action>
  <verify>
    Open http://localhost:8000 in a browser. Page background must be dark (#0f1419 approximation visually). All text is light. No white panels or light input backgrounds.
    Automated check: grep -n "#fff\|#ffffff\|background: white\|background-color: white" static/index.html — must return 0 matches.
  </verify>
  <done>
    CSS :root defines all dark tokens. Every interactive element (input, select, textarea, button, section, list) renders against the dark palette. No light-mode colors remain in the stylesheet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement post-extraction form reset (UI-04)</name>
  <files>static/index.html</files>
  <action>
    Add a resetFormAfterExtract() function to the script block:

    ```javascript
    function resetFormAfterExtract() {
      document.getElementById("filename").value = "";
      document.getElementById("description").value = DESC_TEMPLATE;
      var d = timelineDuration;
      if (d && isFinite(d) && d > 0) {
        applyScrubberRange(0, d);
      }
    }
    ```

    Wire it into onClipSubmit: call resetFormAfterExtract() immediately after a successful POST /api/clips response (after res.ok check passes, before refreshClips()). Do NOT call it on error paths.

    The function must:
    1. Clear the filename field (set to empty string)
    2. Reset description textarea to DESC_TEMPLATE (the multi-line template string defined at top of script)
    3. If timelineDuration is a positive finite number, call applyScrubberRange(0, timelineDuration) to reset handles to full range
    4. Leave start/end timestamp fields untouched (user may want to continue from same position)
    5. Leave the URL field untouched (user is stamping more clips from same video)

    Existing functions required: applyScrubberRange() already moves handles and updates timestamp fields. DESC_TEMPLATE already defined at top of script as a const.
  </action>
  <verify>
    1. Extract a clip with a filename like "test-clip"
    2. After the "Clip saved" status message appears, the filename field must be empty
    3. Description textarea must show the DESC_TEMPLATE text (Source: / Tags: / URL: / Range: / Notes: lines)
    4. If a video was loaded, scrubber handles must be at 0% and 100% positions
    5. Start/end fields remain at their previous values (not cleared)
    No page reload should occur.
  </verify>
  <done>
    resetFormAfterExtract() exists and is called on successful extraction. Filename clears. Description resets to template. Scrubber resets to full range when duration is known. All verified without page reload.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -n "#fff\|#ffffff\|background: white" static/index.html` returns no matches
2. `grep -n "resetFormAfterExtract" static/index.html` returns at least 2 matches (definition + call site)
3. App loads at http://localhost:8000 with dark background visible immediately
4. Full extraction flow: paste URL, prepare, set timestamps + filename, extract — filename clears after success
</verification>

<success_criteria>
1. Opening the app shows dark background (#0f1419) with light text — no white or light-grey backgrounds anywhere
2. All interactive elements render consistently within the dark theme (inputs dark-filled, buttons styled with token colors)
3. After extraction completes, filename field is empty and form is ready for next clip without page reload
</success_criteria>

<output>
After completion, create `.planning/phases/05-dark-ui-overhaul/05-01-SUMMARY.md`
</output>
